# Vercel Serverless 依赖
flask>=2.3.0
flask-cors>=4.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
requests>=2.28.0
aiohttp>=3.9.0

# PDF Generation (可选)
# WeasyPrint 需要系统依赖，Vercel 可能不支持

# 开发依赖
python-dotenv>=1.0.0